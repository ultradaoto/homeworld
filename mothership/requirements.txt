anthropic>=0.25.0
python-dotenv>=1.0.0
chromadb>=0.5.0
rich>=13.0.0
typer>=0.12.0
aiofiles>=23.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
markdownify>=0.12.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
requests>=2.31.0
pynput>=1.7.6
# Voice I/O (optional — all degrade gracefully if absent)
# pyaudio>=0.2.14        # mic recording
# openai>=1.0.0          # Whisper API STT
# SpeechRecognition>=3.10 # system STT fallback
# pyttsx3>=2.90          # TTS fallback (Windows SAPI / Linux espeak)
